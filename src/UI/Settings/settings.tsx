import React, {FC}  from "react";

import "./settings.css";

const Settings: FC = () => {
    return (
        <div className="settings">
            Settings are WIP
        </div>
    );
};

export default Settings;
