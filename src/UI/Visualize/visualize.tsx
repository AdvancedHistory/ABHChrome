import React, {FC}  from "react";

import "./visualize.css";

const Visualize: FC = () => {
    return (
        <div className="visualize">
            Visualization is a WIP
        </div>
    );
};

export default Visualize;
